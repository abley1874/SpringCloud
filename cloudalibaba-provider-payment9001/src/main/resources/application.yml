server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #指定Nacos地址

management:
  endpoints:
    web:
      exposure:     #暴露端口号
        include: '*'
